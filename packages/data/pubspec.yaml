name: git_pilot_data
description: Data and infrastructure layer for Git Pilot.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.1

dependencies:
  git_pilot_core:
    path: ../core
  git_pilot_domain:
    path: ../domain
  process: ^5.0.5

dev_dependencies:
  lints: ^6.0.0
